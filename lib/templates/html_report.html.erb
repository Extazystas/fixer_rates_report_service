<!DOCTYPE html>
<html>
  <head>
    <style>
      body {
        display: flex;
        align-items: center;
        justify-content: center;
        flex-direction: column;
        background-color: #aeaeae;
        color: #000552;
        font-family: "Open Sans";
      }

      table {
        border: 1px solid black;
        border-collapse: collapse;
        margin-bottom: 20px;
      }

      table td, table th {
        border: 1px solid black;
        padding: 10px;
      }

      .content {
        display: flex;
        align-items: center;
        justify-content: center;
        flex-direction: column;
      }

      .footer {
        margin-top: 50px;
        font-size: 0.8em;
      }
    </style>
  </head>
  <body>
    <div class='content'>
      <h1>This is currencies report based on Fixer currency rates. Freska test cunsumer app.</h1>
      <p>Below is provided current value and delta for next dates:</p>

        <table>
          <thead>
            <th>Dates</th>
            <% @currencies.each do |currency| %>
              <th><%= "#{@base_currency}/#{currency}" %></th>
            <% end %>
          </thead>

          <tbody>
            <tr>
              <td><%= @current_date %></td>
              <% @current_rates.each do |_, val| %>
                <td><%= val %></td>
              <% end %>
            </tr>

            <% @comparable_rates.each do |rate_info| %>
              <tr>
                <td><%= rate_info['date'] %></td>
                <% rate_info['rates'].each do |currency, val| %>
                  <td><%= (val - @current_rates[currency]).round(6) %></td>
                <% end %>
              </tr>
            <% end %>
          </tbody>
        </table>
    </div>
    <div class='footer'>
      Made by <a href='https://github.com/extazystas' target='_blank'>Stanislav Knyazev</a>.
    </div>
  </body>
</html>
